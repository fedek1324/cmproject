<?xml version='1.0' encoding='UTF-8'?>
<ui xmlns="http://www.intertrust.ru/schema/palette/tn-ui">
  <form schema-ref="DateInterval" id="DialogDateInterval" projection="default" group="true">
    <component>
      <field attribute-ref="Start" name="${ui.form.DialogDateInterval.field.Start.name:Старт}">
        <properties>
          <require condition-union="AND">
            <condition>true</condition>
          </require>
        </properties>
      </field>
      <field attribute-ref="End" name="${ui.form.DialogDateInterval.field.End.name:Конец}">
        <properties>
          <require condition-union="AND">
            <condition>true</condition>
          </require>
        </properties>
      </field>
    </component>
  </form>
</ui>