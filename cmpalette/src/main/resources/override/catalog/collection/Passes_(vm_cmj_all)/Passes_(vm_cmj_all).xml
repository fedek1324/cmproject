<collection name="Passes_(vm_cmj_all)" idField="id" replace="runtime">
    <prototype>
        <![CDATA[select
	id,
	created_date,
	updated_date,
	module,
	self_1,
	self_2,
	self_3,
	reason,
	giveDateTime,
	forWho,
	toWho,
	carNumber,
	carModel
from
	(
	select
		rkkbase.id,
		rkkbase.created_date,
		rkkbase.updated_date,
		rkkbase.module as module,
		'<id>' as self_1,
		':' as self_2,
		'</>' as self_3,
		(
		select
			tfs."value" as v
		from
			tn_field
		join tn_field_string tfs on
			tn_field.access_object_id = tfs.id
		where
			tn_field."owner" = rkkbase.id
			and tn_field.owner_type = rkkbase.id_type
			and tn_field.cmjfield = 'reason') as reason,
		(
		select
			tfs."value" as v
		from
			tn_field
		join tn_field_datetime tfs on
			tn_field.access_object_id = tfs.id
		where
			tn_field."owner" = rkkbase.id
			and tn_field.owner_type = rkkbase.id_type
			and tn_field.cmjfield = 'giveDateTime') as giveDateTime,
		coalesce((select string_agg(SPLIT_PART(tfs.value, '%', 1), ',') as v
		 from tn_field
		 join tn_field_string tfs on tn_field.access_object_id = tfs.id
		 where tn_field."owner" = rkkbase.id and tn_field.cmjfield = 'Tn$_forWho'), 'Не указано'
		) as forWho,
				coalesce((select string_agg(SPLIT_PART(tfs.value, '%', 1), ',') as v
		 from tn_field
		 join tn_field_string tfs on tn_field.access_object_id = tfs.id
		 where tn_field."owner" = rkkbase.id and tn_field.cmjfield = 'Tn$_toWho'), 'Не указано'
		) as toWho,
		(
		select
			tfs."value" as v
		from
			tn_field
		join tn_field_string tfs on
			tn_field.access_object_id = tfs.id
		where
			tn_field."owner" = rkkbase.id
			and tn_field.owner_type = rkkbase.id_type
			and tn_field.cmjfield = 'carNumber') as carNumber,
		(
		select
			tfs."value" as v
		from
			tn_field
		join tn_field_string tfs on
			tn_field.access_object_id = tfs.id
		where
			tn_field."owner" = rkkbase.id
			and tn_field.owner_type = rkkbase.id_type
			and tn_field.cmjfield = 'carModel') as carModel
	from
		F_DP_Intrkk internalrkk
	join f_dp_rkk rkk on
		rkk.id = internalrkk.id
	join f_dp_rkkbase rkkbase on
		rkkbase.id = rkk.id
	where
		rkkbase.isdeleted <> 1
		and rkk.regnumcnt is null
		   ) s
	where
		1 = 1 ::where-clause
            ]]>
    </prototype>
    <counting-prototype>
        <![CDATA[
            SELECT
				COUNT(1)
                FROM (
                       select
	rkkbase.id,
    rkkbase.created_date,
    rkkbase.updated_date,
    rkkbase.module                                   AS Module,
    '<id>'                                           AS self_1,
    ':'                                              AS self_2,
    '</>'                                            AS self_3
	FROM F_DP_Intrkk internalrkk
                  JOIN f_dp_rkk rkk ON rkk.id = internalrkk.id
                  JOIN f_dp_rkkbase rkkbase ON rkkbase.id = rkk.id
                  JOIN F_DP_Intrkk_Executor executor ON executor.owner = internalrkk.id
                  JOIN so_beard beard ON beard.id = executor.executor
         WHERE rkkbase.isdeleted <> 1
           AND rkk.regnumcnt IS NULL
		   ) s
                WHERE 1=1 ::where-clause
            ]]>
    </counting-prototype>
    <filter name="MODULE">
        <criteria placeholder="where-clause">
            <![CDATA[
                    Module = {0}
                ]]>
        </criteria>
    </filter>
    <filter name="self">
        <criteria placeholder="where-clause"><![CDATA[ Module = {0} and id = {1} ]]></criteria>
    </filter>
    <filter name="created_date">
        <criteria placeholder="where-clause">
            <![CDATA[
                   cast(created_date as date) = cast({0} as date)
                ]]>
        </criteria>
    </filter>
</collection>